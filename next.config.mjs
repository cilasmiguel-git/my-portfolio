// next.config.mjs
/** @type {import('next').NextConfig} */
const nextConfig = {
    // faz o Next gerar a pasta de build como "build" (em vez de ".next")
    distDir: 'build',
    // (opcional) se estiver usando app router e imagens remotas, configure aqui domains, etc.
  };
  
  export default nextConfig;
  